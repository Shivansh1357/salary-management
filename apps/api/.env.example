# SQLite database file (relative to apps/api). For Render, point at the mounted
# disk, e.g. file:/var/data/prod.db
DATABASE_URL="file:./dev.db"

# Port the API listens on.
PORT=4000

# Comma-separated allowed web origins for CORS (the deployed web URL in prod).
CORS_ORIGIN="http://localhost:5173"
